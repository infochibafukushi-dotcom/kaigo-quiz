# 現状分析レポート（2026-05-15）

## 1. リポジトリ構成
- フロントエンドは `index.html` + `style.css` + `app.js` の静的構成。
- 問題データは `questions.json` を読み込み、`localStorage` 上書きに対応。
- 運用ドキュメントは `README.md` に最小限の記載のみ。

## 2. アプリ仕様（実装ベース）
- 初期表示で `questions.json` を取得後、ローカル保存データがあれば優先する実装。
- 出題形式の表示対応は以下 5 種：
  - `fill`
  - `fill_multi`
  - `ox`
  - `choice`
  - `multi`
- 採点はクライアント側で即時判定。

## 3. データ現況サマリ（questions.json）
- コース数: 1
- 単元数: 11
- 総問題数: 133
- 出題形式内訳:
  - `ox`: 67
  - `fill_multi`: 30
  - `fill`: 16
  - `choice`: 12
  - `multi`: 6
  - `choice_multiple`: 1
  - `match`: 1

## 4. 重要ギャップ（動作不整合）
`questions.json` には以下の形式が存在するが、`app.js` の描画分岐に未実装。
- `choice_multiple`（1問）
- `match`（1問）

このため、該当問題では入力UIが出ず、回答不能になる可能性が高い。

## 5. 運用上の現状評価
- GitHub Pages の静的運用前提としてはシンプルで保守しやすい。
- ただし、データ形式と描画形式の不一致があり、教材としての完走性にリスク。
- `README.md` の運用手順は最低限で、
  - 問題タイプ仕様
  - 追加時のバリデーション観点
  - 障害時切り戻し手順
  が不足。

## 6. 優先度つき改善提案
1. **最優先**: `choice_multiple` / `match` のUI実装、またはデータ側を既存5形式へ正規化。
2. **高**: 起動時に「未対応問題タイプ検出」の警告を出す簡易バリデーション追加。
3. **中**: `README.md` に問題タイプ仕様と更新手順（チェックリスト）を追記。
4. **中**: 問題件数・タイプ件数の自動集計スクリプトを追加し、更新時に再確認可能にする。

## 7. 結論
現行アプリは「基本機能は成立」している一方、データ拡張に伴う型不整合がすでに発生しており、
**実運用上は“部分的に回答不能問題を含む状態”**。
まずは問題タイプ不整合の解消が、品質改善の最短ルート。
